class PostStation {
  final String title;
  final String type;
  final String city;
  final String address;
  final int distance;
  final String desc;
  final int price;

  PostStation(this.title, this.type, this.city, this.address, this.distance,
      this.desc, this.price);
}
